<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="DarkTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorPrimary">@android:color/background_dark</item>
        <item name="colorPrimaryDark">@android:color/background_dark</item>
        <item name="colorAccent">@android:color/holo_blue_dark</item>
        <item name="android:windowBackground">@android:color/background_dark</item>
        <!--<item name="android:background">@android:color/background_dark</item>-->
        <!--<item name="colorPrimary">@color/colorPrimaryRed</item>-->
        <!--<item name="colorPrimaryDark">@color/colorPrimaryDarkRed</item>-->
        <!--<item name="colorAccent">@color/colorAccentRed</item>-->
    </style>

    <style name="DarkTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="DarkTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="DarkTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>
    
    <style name="AlertDialogCustom" parent="AlertDialogCustom">
        <!--<item name="android:textColor">@android:color/primary_text_light</item>-->
        <item name="android:background">#CC111111</item>
    </style>


    <style name="ProgressBarCustom" parent="ProgressBarCustom">
        <item name="android:textColor">@android:color/secondary_text_light</item>
        <!--<item name="android:background">#CC111111</item>-->
    </style>


    <style name="styleWidgetText" parent="android:Widget.TextView">
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:textSize">4pt</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>